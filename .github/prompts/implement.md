# Implementation Phase

## Issue Context
Processing issue #${ISSUE_NUMBER}: ${ISSUE_TITLE}

### Issue Description
${ISSUE_BODY}

## Working Directory for This Issue
`${PROJECT_DIR}`

## Your Task
You are in the **Implementation Phase**.

Implement the design under `${PROJECT_DIR}` and ensure tests exist for behavior changes.

Create or update:
1. `${PROJECT_DIR}/src/**`
2. `${PROJECT_DIR}/tests/**`
3. `${PROJECT_DIR}/implementation/implementation-notes.md`

## Requirements
- Follow `${PROJECT_DIR}/design/architecture.md` if present.
- If design is incomplete, make minimal assumptions and document them in implementation notes.
- Keep changes small, coherent, and in scope.
- Add or update tests for new behavior and failure paths.
- Do not modify unrelated project directories.

## PR Context
The workflow will commit and open/update a PR after this stage.
Write implementation notes suitable for PR reviewers.
