# Projects Directory

Each workflow issue is mapped to one project folder:

- `projects/<issue-title-kebab-case>/`

The pipeline keeps all issue-specific artifacts and code inside that project folder.
